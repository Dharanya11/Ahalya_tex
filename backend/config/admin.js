// Single-admin rule configuration.
// Only this email can ever have admin privileges in the whole app.

export const ADMIN_EMAIL = 'admin@example.com';
export const ADMIN_DEFAULT_PASSWORD = 'admin123';

